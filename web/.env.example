# Point the web client at your signaling server.
# Defaults to http://<current-hostname>:4000 when unset, which works for both
# localhost and phones on the same LAN.
VITE_SERVER_URL=http://localhost:4000
